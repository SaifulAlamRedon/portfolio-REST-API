{
  "name": "portfolio-root",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "npm --prefix portfolio run start",
    "start:dev": "npm --prefix portfolio run start:dev",
    "start:prod": "npm --prefix portfolio run start:prod",
    "build": "npm --prefix portfolio run build",
    "test": "npm --prefix portfolio run test"
  },
  "dependencies": {
    "@nestjs/config": "^4.0.4",
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/passport": "^11.0.5",
    "@nestjs/typeorm": "^11.0.3",
    "bcrypt": "^6.0.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "passport": "^0.7.0",
    "passport-jwt": "^4.0.1",
    "typeorm": "^1.0.0"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jest": "^30.0.0",
    "@types/passport-jwt": "^4.0.1"
  }
}
